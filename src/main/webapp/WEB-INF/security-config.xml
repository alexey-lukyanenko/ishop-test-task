<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
        xmlns:beans="http://www.springframework.org/schema/beans"
        xmlns="http://www.springframework.org/schema/security"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
                            http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
                            http://www.springframework.org/schema/security
                            http://www.springframework.org/schema/security/spring-security-4.0.xsd"
>
    <http pattern="/resources/**" security="none" />

    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/users/**" access="hasAuthority('admin')"/>
        <intercept-url pattern="/customers/**" access="hasAuthority('admin')"/>
        <intercept-url pattern="/category/**" access="hasAuthority('admin')"/>
        <intercept-url pattern="/goods/*/edit" access="hasAuthority('admin')"/>
        <intercept-url pattern="/goods" access="hasAuthority('admin')" method="POST"/>
        <intercept-url pattern="/goods" access="hasAuthority('admin')" method="PUT"/>
        <intercept-url pattern="/goods" access="hasAuthority('admin')" method="DELETE"/>
        <intercept-url pattern="/**" access="permitAll"/>
        <logout logout-success-url="/" logout-url="/logout" />
    </http>
    <authentication-manager id="authenticationManager">
        <authentication-provider>
            <jdbc-user-service
                    id="userService"
                    data-source-ref="dataSource"
                    users-by-username-query="select name, password, true from app_user where name = lower(?)"
                    authorities-by-username-query="select user_name, role_name from app_user_role where user_name = lower(?)"
            />
        </authentication-provider>
    </authentication-manager>


</beans:beans>